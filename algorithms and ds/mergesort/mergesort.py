'''
Author : Unnati Patel
Language : Python
Algorithm : Merge Sort
'''


def Merge(A,l,r):
    k = 0
    i = 0
    j = l
    while(k != i + j - 1)
    {
        if (A[i] <= A[j] && i!=l)
        {
            A[k] = A [i]
            i++
            k++
        }
        else{
            A[k] = A[j]
        }
    }
    return (result)


def Merge(A,i):
    #pick pivot
    Mergesort(left)
    Mergesort(right)
    Merge(left,right)


    #sort left

    #sort right
    #Merge
